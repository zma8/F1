<!DOCTYPE html>
<html>
<head>
    <title>F1 Race Calendar</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container">
        <h1 class="page-title">🏁 F1 Race Calendar</h1>
        <p class="page-subtitle">2025 Season</p>
        
        <div class="races-grid">
            <% races.forEach(race => { %>
                <div class="race-card">
                    <div class="race-card-header">
                        <h3 class="race-card-title"><%= race.name %></h3>
                        <% if (race.status === 'upcoming') { %>
                            <span class="badge-upcoming">UPCOMING</span>
                        <% } else if (race.status === 'completed') { %>
                            <span class="badge-completed">COMPLETED</span>
                        <% } else { %>
                            <span class="badge-live">LIVE</span>
                        <% } %>
                    </div>
                    
                    <div class="race-card-body">
                        <p class="race-detail">📅 <%= race.date.toDateString() %></p>
                        <p class="race-detail">🏎️ <%= race.circuit %></p>
                        <p class="race-detail">📍 <%= race.country %></p>
                    </div>
                    
                    <div class="race-card-actions">
                        <a href="/races/<%= race._id %>" class="btn-primary">View Details</a>
                        
                        <% if (race.status === 'upcoming') { %>
                            <a href="/races/<%= race._id %>/predictions/new" class="btn-secondary">Make Prediction</a>
                        <% } %>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

</body>
</html>