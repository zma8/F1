<!DOCTYPE html>
<html>
<head>
    <title><%= race.name %> - Admin</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../../partials/navbar') %>

    <div class="container">
        <h1 class="page-title"><%= race.name %></h1>
        
        <div class="details-card">
            <div class="race-info-header">
                <h2 class="section-subtitle">Race Information</h2>
                <% if (race.status === 'upcoming') { %>
                    <span class="badge-upcoming">UPCOMING</span>
                <% } else if (race.status === 'completed') { %>
                    <span class="badge-completed">COMPLETED</span>
                <% } else { %>
                    <span class="badge-live">LIVE</span>
                <% } %>
            </div>

            <div class="race-info-details">
                <p class="race-detail">📅 <%= race.date.toDateString() %></p>
                <p class="race-detail">🏎️ <%= race.circuit %></p>
                <p class="race-detail">📍 <%= race.country %></p>
            </div>

            <div class="details-section">
                <h3 class="section-subtitle">Race Results</h3>
                <% if (race.pole || race.winner || race.fastestLap) { %>
                    <div class="details-grid">
                        <div class="detail-item">
                            <span class="detail-label">Pole Position:</span>
                            <span class="detail-value"><%= race.pole || 'Not set' %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Winner:</span>
                            <span class="detail-value"><%= race.winner || 'Not set' %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Podium:</span>
                            <span class="detail-value"><%= race.podium && race.podium.length > 0 ? race.podium.join(', ') : 'Not set' %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Fastest Lap:</span>
                            <span class="detail-value"><%= race.fastestLap || 'Not set' %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">First DNF:</span>
                            <span class="detail-value"><%= race.firstDNF || 'Not set' %></span>
                        </div>
                    </div>
                <% } else { %>
                    <p class="empty-message">No results entered yet</p>
                <% } %>
            </div>

            <div class="form-actions">
                <a href="/admin/races/<%= race._id %>/edit" class="btn-primary">Edit Race</a>
                <a href="/admin/races" class="btn-secondary">Back to Races</a>
                <form method="POST" action="/admin/races/<%= race._id %>?_method=DELETE" style="display: inline;">
                    <button type="submit" class="btn-delete" onclick="return confirm('Are you sure you want to delete this race?')">Delete Race</button>
                </form>
            </div>
        </div>
    </div>

</body>
</html>