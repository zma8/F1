<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>F1 Race Weekend Planner</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <% if (user) { %>
        <%- include('partials/navbar') %>
        <div class="container">
            <h1 class="welcome-title">Welcome back, <%= user.username %>!</h1>
            <div class="form-actions" >
                <a href="/dashboard" class="btn-primary">Go to Dashboard</a>
            </div>
        </div>
    <% } else { %>
        <div class="landing-container">
            <div class="landing-content">
                <h1 class="landing-title">RACE WEEKEND <span>PLANNER</span></h1>
                <p class="landing-subtitle">Predict F1 race outcomes and compete with friends</p>
                <div class="landing-actions">
                    <a href="/auth/sign-up" class="btn-primary">Sign Up</a>
                    <a href="/auth/sign-in" class="btn-secondary">Sign In</a>
                </div>
            </div>
        </div>
    <% } %>
</body>
</html>